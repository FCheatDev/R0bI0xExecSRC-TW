<UserControl x:Class="Executor.WaveUI.WaveViews.KeySystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wave="clr-namespace:Executor.WaveUI"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="BounceButtonStyle" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="0.94" Duration="0:0:0.06" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="0.94" Duration="0:0:0.06" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.18">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.18">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IconLinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="UnderlineTextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#EDEDED" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <ContentPresenter x:Name="ContentHost" HorizontalAlignment="Left" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}" />
                                <Rectangle x:Name="Underline" Height="2" Fill="#EDEDED" HorizontalAlignment="Left" Margin="0,2,0,-4"
                                           Width="{Binding ActualWidth, ElementName=ContentHost}" RenderTransformOrigin="0,0.5">
                                    <Rectangle.RenderTransform>
                                        <ScaleTransform x:Name="UnderlineScale" ScaleX="0" ScaleY="1" />
                                    </Rectangle.RenderTransform>
                                </Rectangle>
                            </StackPanel>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White" />
                                <Setter TargetName="Underline" Property="Fill" Value="White" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="UnderlineScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.28" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="UnderlineScale" Storyboard.TargetProperty="ScaleX" To="0" Duration="0:0:0.28" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#000000" MouseLeftButtonDown="Root_OnMouseLeftButtonDown">

        <Button x:Name="SkipButton" Style="{StaticResource UnderlineTextButtonStyle}" Click="SkipButton_OnClick" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Visibility="Collapsed">
            <TextBlock x:Name="SkipButtonText" Text="Skip" FontWeight="SemiBold" FontSize="16" />
        </Button>

        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="340">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <wave:WaveIcon x:Name="LockIcon" IconName="lock" Width="28" Height="28" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,14" />

            <TextBlock x:Name="TitleText" Grid.Row="1" Text="Wave Key System" Foreground="#EDEDED" FontWeight="SemiBold" FontSize="18" HorizontalAlignment="Center" FontFamily="{DynamicResource WaveHeadingFontFamily}" />
            <TextBlock x:Name="SubtitleText" Grid.Row="2" Text="Purchase a key from our official&#x0a;store, or from one of our resellers." Foreground="#9AA0A6" TextAlignment="Center" FontSize="12" Margin="0,12,0,0" FontFamily="{DynamicResource WaveSubtitleFontFamily}" />

            <Grid Grid.Row="3" Margin="0,18,0,0">
                <Border x:Name="KeyBoxBorder" Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="6" Height="28" Width="220" HorizontalAlignment="Center" MouseLeftButtonDown="KeyBoxBorder_OnMouseLeftButtonDown">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#141414" />
                            <Setter Property="BorderBrush" Value="#2A2A2A" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=KeyBox}" Value="True">
                                    <Setter Property="Background" Value="#2b2b2b" />
                                    <Setter Property="BorderBrush" Value="#4e4e4e" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <TextBox x:Name="KeyBox" Background="Transparent" Foreground="#EDEDED" CaretBrush="#FFFFFF" BorderThickness="0" Margin="12,0,32,0" FontSize="12" VerticalContentAlignment="Center" FontFamily="{DynamicResource WaveHeadingFontFamily}" FontWeight="Bold" />
                        <wave:WaveTintIcon IconName="key" TintBrush="White" Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.9" />
                    </Grid>
                </Border>
            </Grid>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                <Button x:Name="LoginButton" Style="{StaticResource BounceButtonStyle}" Width="86" Height="30" Background="#1DA1F2" Foreground="White" BorderThickness="0" Cursor="Hand" Click="Login_OnClick" FontFamily="{DynamicResource WaveTitleFontFamily}" FontSize="14">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock x:Name="LoginButtonText" Text="Log in" FontWeight="SemiBold" FontSize="12" />
                </Button>

                <Button Style="{StaticResource BounceButtonStyle}" Width="86" Height="30" Background="#1A1A1A" Foreground="#EDEDED" BorderThickness="0" Cursor="Hand" Margin="10,0,0,0" FontSize="14" FontFamily="{DynamicResource WaveTitleFontFamily}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock x:Name="GetKeyButtonText" Text="Get Key" FontWeight="SemiBold" FontSize="12" FontFamily="{DynamicResource WaveTitleFontFamily}" />
                </Button>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,14" Opacity="0.75">
            <Button Style="{StaticResource IconLinkButtonStyle}" Cursor="Hand" Click="YouTube_OnClick">
                <wave:WaveIcon IconName="youtube" Width="14" Height="14" Stretch="Uniform" />
            </Button>
            <Button Style="{StaticResource IconLinkButtonStyle}" Cursor="Hand" Margin="14,0,0,0" Click="Discord_OnClick">
                <wave:WaveIcon IconName="discord" Width="14" Height="14" Stretch="Uniform" />
            </Button>
            <Button Style="{StaticResource IconLinkButtonStyle}" Cursor="Hand" Margin="14,0,0,0" Click="Global_OnClick">
                <wave:WaveIcon IconName="globe" Width="14" Height="14" Stretch="Uniform" />
            </Button>
        </StackPanel>

        <Grid x:Name="VerifyOverlay" Background="#B0000000" Visibility="Collapsed" Opacity="0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid Width="52" Height="52">
                    <Ellipse Stroke="#404040" StrokeThickness="5" />
                    <Path x:Name="SpinnerArc" Stroke="#EDEDED" StrokeThickness="5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 26,3 A 23,23 0 0 1 49,26" />
                </Grid>
                <TextBlock x:Name="VerifyingText" Text="Verifying..." Foreground="#EDEDED" FontSize="16" HorizontalAlignment="Center" Margin="0,12,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
