<UserControl x:Class="Executor.WaveUI.WaveViews.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#0C0C0C" Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" CornerRadius="10" Background="#0B0B0B" BorderBrush="#202020" BorderThickness="1" />

        <Border Grid.Column="1" CornerRadius="10" Background="Black" BorderBrush="#202020"  Margin="10,0,0,0">
            <Grid Margin="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="CLIENTS" Foreground="#EDEDED" FontWeight="SemiBold" FontSize="14" FontFamily="Arial Black" />
                <TextBlock Grid.Row="1" Text="Client manager used for Wave to&#10;manage your current sessions" Foreground="#9AA0A6" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap" />

                <Button Grid.Row="3" Content="Select All" Width="90" Height="32" HorizontalAlignment="Right" Background="#1DA1F2" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="9">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
