    <UserControl x:Class="Executor.WaveUI.WaveViews.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wave="clr-namespace:Executor.WaveUI"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

        <UserControl.Resources>
            <Style x:Key="BounceButtonStyle" TargetType="Button">
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="1" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="12">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=RenderTransform.(ScaleTransform.ScaleX)}"
                                                    ScaleY="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=RenderTransform.(ScaleTransform.ScaleY)}" />
                                </Border.RenderTransform>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.94" Duration="0:0:0.06" />
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.94" Duration="0:0:0.06" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.18">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.18">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </UserControl.Resources>

    <Grid Background="#000000" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="72" />
        </Grid.RowDefinitions>

        <Grid x:Name="EditorHost" Grid.Row="0" Grid.RowSpan="3" Margin="0,0,280,0" Background="#0C0C0C">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="72" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="0" Background="#0C0C0C" BorderBrush="#202020" BorderThickness="1,1,1,0" Padding="10" Margin="0">
                <DockPanel>
                    <Grid DockPanel.Dock="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ScrollViewer x:Name="TabScrollViewer" Grid.Column="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" PreviewMouseWheel="TabScrollViewer_OnPreviewMouseWheel">
                            <ItemsControl x:Name="TabsItems" ItemsSource="{Binding Tabs}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="10,6" Margin="0,0,8,0" Cursor="Hand" MouseLeftButtonDown="TabBorder_OnMouseLeftButtonDown">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="Transparent" />
                                                    <Setter Property="CornerRadius" Value="0" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="Background" Value="#44A3FC" />
                                                            <Setter Property="CornerRadius" Value="5" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <Rectangle Grid.ColumnSpan="3" Height="2" VerticalAlignment="Bottom" Fill="#1DA1F2">
                                                    <Rectangle.Style>
                                                        <Style TargetType="Rectangle">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Rectangle.Style>
                                                </Rectangle>

                                                <Ellipse Width="8" Height="8" Opacity="0.95" VerticalAlignment="Center">
                                                    <Ellipse.Style>
                                                        <Style TargetType="Ellipse">
                                                            <Setter Property="Fill" Value="#EDEDED" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                    <Setter Property="Fill" Value="#FFFFFF" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Ellipse.Style>
                                                </Ellipse>

                                                <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" FontSize="12" Margin="5,0,10,0" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#EDEDED" />
                                                            <Setter Property="Visibility" Value="Visible" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                    <Setter Property="Foreground" Value="#FFFFFF" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <TextBox Grid.Column="1" Text="{Binding EditingTitle, UpdateSourceTrigger=PropertyChanged}" FontSize="12" Margin="5,0,10,0" VerticalAlignment="Center"
                                                         Background="#0E0E0E" Foreground="#FFFFFF" BorderBrush="#2A2A2A" BorderThickness="1" Padding="4,2"
                                                         Loaded="TabRenameBox_OnLoaded" KeyDown="TabRenameBox_OnKeyDown" LostFocus="TabRenameBox_OnLostFocus">
                                                    <TextBox.Style>
                                                        <Style TargetType="TextBox">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBox.Style>
                                                </TextBox>

                                                <Button Grid.Column="2" Width="18" Height="18" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="TabClose_OnClick" Padding="0">
                                                    <wave:WaveIcon IconName="editor_tab_close" Width="12" Height="12" Stretch="Uniform" Margin="0" />
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Border Grid.Column="1" Background="Transparent" CornerRadius="0" Padding="10,6" Margin="0,0,8,0" Cursor="Hand" MouseLeftButtonDown="AddTab_OnMouseLeftButtonDown">
                            <wave:WaveIcon IconName="editor_tab_add" Width="12" Height="12" Stretch="Uniform" />
                        </Border>
                    </Grid>

                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <wave:WaveIcon IconName="grid" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0" OpacityMask="Black" />
                        <wave:WaveIcon IconName="chat" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" CornerRadius="0" Background="#0C0C0C" BorderBrush="#202020" BorderThickness="1,0,1,1" Margin="0,0,0,10">
                <wv2:WebView2 x:Name="MonacoView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            </Border>

            <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,0,3,10" Background="#0C0C0C">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button x:Name="ExecuteButton" Width="92" Height="36" Background="#42A2F8" Foreground="White" BorderThickness="0" Cursor="Hand" Click="Execute_OnClick" Margin="10,0,10,0" Style="{StaticResource BounceButtonStyle}">
                        <TextBlock Text="Execute" FontWeight="SemiBold" FontSize="12" />
                    </Button>

                    <Button Width="74" Height="36" Background="#191A1B" Foreground="#EDEDED" BorderThickness="0" Cursor="Hand" Margin="0" Click="Clear_OnClick" Style="{StaticResource BounceButtonStyle}">
                        <TextBlock Text="Clear" FontWeight="SemiBold" FontSize="12" />
                    </Button>

                    <Button Width="74" Height="36" Background="#191A1B" Foreground="#EDEDED" BorderThickness="0" Cursor="Hand" Margin="10,0,0,0" Style="{StaticResource BounceButtonStyle}">
                        <TextBlock Text="Open" FontWeight="SemiBold" FontSize="12" />
                    </Button>

                    <Button Width="74" Height="36" Background="#191A1B" Foreground="#EDEDED" BorderThickness="0" Cursor="Hand" Margin="10,0,0,0" Style="{StaticResource BounceButtonStyle}">
                        <TextBlock Text="Save" FontWeight="SemiBold" FontSize="12" />
                    </Button>
                </StackPanel>

                <Button x:Name="ExplorerToggleButton" DockPanel.Dock="Right" Width="36" Height="36" Background="#191A1B" Foreground="#EDEDED" BorderThickness="0" Cursor="Hand" Click="ExplorerToggle_OnClick" Margin="10,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="12" BorderBrush="#202020" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Image x:Name="ExplorerToggleIcon" Width="16" Height="16" Opacity="1" />
                </Button>
            </DockPanel>

        </Grid>

        <Border x:Name="ExplorerPanel" Width="280" HorizontalAlignment="Right" Grid.Row="0" Grid.RowSpan="3" CornerRadius="0" Background="#000000" BorderThickness="0" RenderTransformOrigin="0,0.5">
            <Border.RenderTransform>
                <TranslateTransform x:Name="ExplorerTransform" X="0" Y="0" />
            </Border.RenderTransform>
            <Grid Margin="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="EXPLORER" Foreground="#EDEDED" FontSize="14" FontWeight="SemiBold" FontFamily="Arial Black" UseLayoutRounding="True" />
                <TextBlock Grid.Row="1" Text="Explore all your saved scripts" Foreground="#9AA0A6" FontSize="12" Margin="0,6,0,0" FontFamily="Noto Sans HK Black" />

                <Border Grid.Row="2" Background="#0E0E0E" BorderBrush="#202020" BorderThickness="1" CornerRadius="0" Height="36" Padding="12,0" Margin="0,14,0,0" VerticalAlignment="Center">
                    <Grid>
                        <TextBlock Text="" />
                        <wave:WaveIcon IconName="search" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Right" VerticalAlignment="Center" Opacity="0.8" />
                    </Grid>
                </Border>

                <StackPanel Grid.Row="3" Margin="0,14,0,0">
                    <Border CornerRadius="0" BorderBrush="#202020" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <wave:WaveIcon IconName="right" Width="14" Height="14" Stretch="Uniform" Opacity="0.9" VerticalAlignment="Center">
                                <wave:WaveIcon.RenderTransform>
                                    <RotateTransform Angle="90" CenterX="7" CenterY="7" />
                                </wave:WaveIcon.RenderTransform>
                            </wave:WaveIcon>
                            <TextBlock Grid.Column="1" Text="Saved Scripts" Foreground="#EDEDED" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0" FontFamily="Arial Black" />
                            <Border Grid.Column="2" Background="#1DA1F2" CornerRadius="4,4,4,4" Width="22" Height="22" HorizontalAlignment="Right">
                                <wave:WaveIcon IconName="explorer_add" Width="12" Height="12" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
